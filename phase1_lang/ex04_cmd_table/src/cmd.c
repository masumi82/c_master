#include "cmd.h"

int cmd_dispatch(const cmd_entry_t *table, size_t table_len, char *line, char *out, size_t out_len)
{
    /* TODO: implement */
    (void)table;
    (void)table_len;
    (void)line;
    (void)out;
    (void)out_len;
    return CMD_OK;
}
